language: csharp
solution: Macros.sln

sudo: required
jdk:
  - oraclejdk8
install: true
script:
  - sudo apt-get update && sudo apt-get install oracle-java8-installer
  - java -version
  - xbuild /p:Configuration=Release Macros.sln
